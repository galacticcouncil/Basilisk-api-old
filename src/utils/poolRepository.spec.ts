import {
    increasePoolBalanceForAssetId,
    decreasePoolBalanceForAssetId,
} from "./poolRepository";

describe("utils/poolRepository", () => {
    describe("increasePoolBalanceForAssetId", () => {
        it.skip("should increase balance for assetA, not assetB", () => {});
        it.skip("should increase balance for assetB. not assetA", () => {});
    });

    describe("decreasePoolBalanceForAssetId", () => {
        it.skip("should decrease balance for assetA, not assetB", () => {});
        it.skip("should decrease balance for assetB, not assetA", () => {});
    });
});
